name: deploy
on:
  push:
    branches: ["main"]
jobs:
  deploy:
    uses: sam-stuhl/console/.github/workflows/app-deploy.yml@main
    permissions:
      id-token: write
      packages: write
      contents: read