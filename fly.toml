app = "notion-academic-sync"
primary_region = "ewr"

[build]

[http_service]
internal_port = 8000
force_https = true
auto_stop_machines = "stop"
auto_start_machines = true

[[vm]]
memory = "256mb"
cpu_kind = "shared"
cpus = 1
